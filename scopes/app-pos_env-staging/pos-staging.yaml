name: pos | staging
description: ''
enabled: true
scopes:
- - label:
      app: pos
  - label:
      env: staging
rules:
- name: 'All Workloads → All Workloads : https'
  enabled: true
  consumers:
  - actors: ams
  providers:
  - actors: ams
  services:
  - name: https
