name: assetmgmt | prod
description: null
enabled: true
scopes:
- - label:
      app: assetmgmt
  - label:
      env: prod
rules:
- name: 'ordering,dev,processing → db : mysql (extra-scope)'
  enabled: true
  consumers:
  - label:
      app: ordering
  - label:
      env: dev
  - label:
      role: processing
  providers:
  - label:
      role: db
  services:
  - name: mysql
  unscoped_consumers: true
