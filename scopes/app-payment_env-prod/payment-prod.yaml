name: payment | prod
description: ''
enabled: true
scopes:
- - label:
      app: payment
  - label:
      env: prod
rules:
- name: 'All Workloads → All Workloads : All Services'
  enabled: true
  consumers:
  - actors: ams
  providers:
  - actors: ams
  services:
  - name: All Services
    proto: '-1'
