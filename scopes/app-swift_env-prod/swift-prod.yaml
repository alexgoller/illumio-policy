name: swift | prod
description: null
enabled: true
scopes:
- - label:
      app: swift
  - label:
      env: prod
rules:
- name: 'All Workloads → All Workloads : https'
  enabled: true
  consumers:
  - actors: ams
  providers:
  - actors: ams
  services:
  - name: https
