name: ordering | prod
description: ''
enabled: true
scopes:
- - label:
      app: ordering
  - label:
      env: prod
rules:
- name: 'All Workloads → All Workloads : All Services'
  enabled: true
  consumers:
  - actors: ams
  providers:
  - actors: ams
  services:
  - name: All Services
    proto: '-1'
- name: 'All Workloads → All Workloads : All Services'
  enabled: true
  consumers:
  - actors: ams
  providers:
  - actors: ams
  services:
  - name: All Services
    proto: '-1'
