name: ad | prod
description: null
enabled: true
scopes:
- - label:
      app: ad
  - label:
      env: prod
rules:
- name: 'All Workloads → All Workloads : All Services'
  enabled: true
  consumers:
  - actors: ams
  providers:
  - actors: ams
  services:
  - name: All Services
